# INCore

This is a NeoForge 1.21.1 Minecraft mod project.

## Conventions

- Avoid fully qualified types when importing is possible
- Check build with `./gradlew build` after making java code changes
- Run tests with `./gradlew test` and `./gradlew runGameTestServer` when making substantial changes
